Use `bd` for task tracking.

# Prompt completion verification

- Before every final response for work in this repository, run `npm run build`.
- After the build succeeds, launch the app with `npm start` and confirm startup.
- Stop the launched app process before the final response unless the user explicitly asks to leave it running.
- If the build or launch cannot run, report the exact blocker and do not mark the work as fully verified.
